services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ticket-classifier-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
